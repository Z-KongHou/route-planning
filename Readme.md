# 智能路线规划系统（React + Vite + 高德地图）

## 项目简介

本项目是一个基于 **React + Vite** 与 **高德地图 JavaScript API** 开发的纯前端“智能路线规划”系统，用户可以输入起点与终点，系统会自动呈现最优路线，并支持路线收藏、编辑、删除以及本地持久化。

---

# 1. 项目目标

- ✔ 实现真实可用的路线规划功能
- ✔ 支持收藏路线的增删改查
- ✔ 支持本地存储（localStorage）
- ✔ 使用 React + Vite 构建 SPA

---

# 2. 核心功能

### 2.1 路线规划功能

- 输入起点、终点
- 支持：驾车 / 步行 / 公交
- 展示路线距离、时间、关键路段
- 地图自动渲染路径与标记点

---

### 2.2 路线收藏（CRUD）

- ⭐ 收藏路线
- ✏ 重命名收藏
- ❌ 删除收藏
- 📄 查看收藏列表

本功能完整覆盖 “增删改查”，满足课程要求。

---

### 2.3 本地数据持久化

使用 `localStorage` 自动保存用户收藏的数据
刷新页面、关机重启后仍存在。

---

### 2.5 多页面视图（React Router）

- 首页：地图 + 搜索 + 路线规划
- 收藏页：管理路线

---

# 3. 技术架构

## 前端技术栈

| 模块     | 技术            |
| -------- | --------------- |
| 框架     | React 18        |
| 构建工具 | Vite            |
| 路由     | React Router    |
| 状态管理 | Context         |
| 地图 API | 高德地图 JS API |
| UI       | Tailwind        |
| 持久化   | localStorage    |

---

# 4. 目录结构

```
├── dist/              # 构建输出目录
│   ├── assets/        # 静态资源
│   └── index.html     # 主HTML文件
├── docs/              # 项目文档
│   ├── 系统架构.md     # 系统架构设计
│   └── 需求说明书.md   # 需求说明文档
├── public/            # 公共静态资源
├── src/               # 源代码目录
│   ├── assets/        # 项目资源
│   ├── pages/         # 页面组件
│   │   ├── AboutPage.jsx  # 关于页面
│   │   └── HomePage.jsx   # 首页（地图 + 路线规划）
│   ├── App.jsx        # 根组件
│   ├── App.css        # 全局样式
│   ├── index.css      # 基础样式
│   ├── main.jsx       # 入口文件
│   └── routes.jsx     # 路由配置
├── .env               # 环境变量配置
├── .gitignore         # Git忽略文件
├── README.md          # 项目说明文档
├── eslint.config.js   # ESLint配置
├── index.html         # HTML模板
├── package.json       # 项目配置
├── pnpm-lock.yaml     # 依赖锁定文件
└── vite.config.js     # Vite配置
```

---

# 5. 系统运行流程（用户视角）

```
用户输入起点和终点 → 系统请求高德路线 → 地图渲染路线 →
用户可选择收藏 → 可管理收藏（编辑/删除） →
系统可依据偏好推荐最优路线
```

---

# 6. 未来扩展方向

- 支持路线多方案比较
- 获取实时交通拥堵信息
- 加入用户登录系统（带后端）
- 收藏同步云端
- 自动推荐“更便宜/更快/更安全的路线”

---

# 7. 快速开始

## 环境要求

- Node.js >= 18
- pnpm（推荐）或 npm/yarn

## 安装依赖

```bash
# 使用 pnpm（推荐）
pnpm install

# 或使用 npm
npm install

# 或使用 yarn
yarn install
```

## 启动开发服务器

```bash
# 使用 pnpm
pnpm dev

# 或使用 npm
npm run dev

# 或使用 yarn
yarn dev
```

开发服务器启动后，访问 `http://localhost:5173` 即可查看应用。

## 构建项目

```bash
# 使用 pnpm
pnpm build

# 或使用 npm
npm run build

# 或使用 yarn
yarn build
```

构建产物将输出到 `dist` 目录。

## **预览构建结果**

```bash
# 使用 pnpm
pnpm preview

# 或使用 npm
npm run preview

# 或使用 yarn
yarn preview
```

## **代码检查**

```bash
# 使用 pnpm
pnpm lint

# 或使用 npm
npm run lint

# 或使用 yarn
yarn lint
```

---
